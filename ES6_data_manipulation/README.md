# ES6 Data Manipulation
This project is part of the JavaScript ES6 curriculum. The main objective is to master data manipulation in modern JavaScript using ES6 array methods (`map`, `filter`, `reduce`), advanced data structures (`Set`, `Map`, `WeakSet`, `WeakMap`), and Typed Arrays.
